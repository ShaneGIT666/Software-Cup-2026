-r requirements.txt

# Optional heavyweight MinerU adapter for document-layout experiments.
# It is not a lightweight base dependency or the selected production parser;
# install it only on explicitly evaluated development/test machines.
mineru[all]==3.2.3
