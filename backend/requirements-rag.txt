-r requirements.txt

# Optional Chroma vector store for non-LoongArch environments where chromadb
# and chroma-hnswlib install cleanly. The competition LoongArch/Kylin route
# uses the built-in SQLite vector store by default.
chromadb>=0.5.0,<2.0.0
