-r requirements.txt

# Optional experimental local OCR adapter for development/evaluation only.
# It is not part of the lightweight base dependency set. Production must use a
# validated real adapter or report OCR unavailable; mock output is forbidden.
rapidocr-onnxruntime>=1.4.0,<2.0.0
