# 复核 M1 前置并冻结身份与审计设计

- 变更标识：`2026-08-13-004-m1-design`
- 日期：`2026-08-13`
- 状态：`已完成`
- 所属模块：`M1`；协作模块：`M0`、`M7`
- 需求追踪：`AUTH-01`～`AUTH-09`、`FR-IAM-01`～`FR-IAM-05`、`DATA-02`～`DATA-05`、`API-01`～`API-04`、`NFR-SEC-02/04/07`、`NFR-OBS-01`～`OBS-03`
- 关联记录：`2026-08-13-002-auth-entry-exception`、`2026-08-13-003-m0-m1-prerequisites`

## 改动内容

- 重新检查 M0 的路由发现、领域模型发现、迁移、分页、CORS、幂等和错误处理，确认 M1 不需要改动 M0 私有目录即可接入。
- 新增 M1 详细设计，定义本地账户基线、OIDC 扩展边界、数据模型、会话/CSRF/密码策略、HTTP API、供 M2/M3/M5 复用的服务端口、实施顺序及冲突解决方案。
- 明确两个公开 API 开工门禁：身份专用错误码须由 M0 登记；审计查看者的业务读取范围须由 SRS 消除矛盾。

## 文件与数据影响

- `docs/design/m1-identity-audit-design.md`；新增：M1 设计、文件/API/表清单与冲突方案。
- `docs/change-log/INDEX.md`；修改：登记本次设计记录。
- `docs/change-log/2026-08-13-004-m1-design.md`；新增：记录复核与设计结论。
- 数据库表、迁移、API、DTO、事件、运行配置：本次未实现；设计计划新增 `users`、`roles`、`user_roles`、`auth_sessions`、`login_throttles`、`audit_events` 和 `APP_AUTH_*` 配置。

## 依赖与冲突检查

- 已检查：前两条 M1/M0 日志、M0 公共契约、SRS 的身份/API/数据需求、M0 的路由/模型发现、迁移链、CORS、分页、幂等及错误码实现，旧 `/api` 审核 DTO 和前端 `reviewer="operator"` 用法。
- 结论：M0 结构性前置已完成并可被 M1 复用；不存在 M1 目录或路由名冲突。发现两项未冻结的契约问题（身份错误码登记、审计查看者读取范围），已在设计中列为 G1/G2 门禁，不会通过私自实现绕过。

## 验证与回滚

- 验证：`pytest tests/test_module0_m1_prerequisites.py tests/test_module0_foundation.py tests/test_configuration_contract.py -q` 为 26 passed；使用临时无凭据 PostgreSQL URL 执行 `alembic upgrade head --sql`，确认 `20260812_0001 -> 20260813_0002` 和 `idempotency_records`；从仓库根目录加载 M0 路由/模型发现成功。已核查当前依赖中未安装 Argon2/OIDC 库，已作为实施前依赖项记录。
- 回滚：删除本次设计文档与本日志，并移除索引行；无代码、数据库、API、配置或运行时数据变更。

## 后续开发提示

- 先处理 G1 与 G2，再开始 M1 的公开路由；可在此之前建立 M1 私有目录、纯 DTO、模型草案、密码/会话单元测试。
- 实施时必须读取 `docs/design/m0-public-contract.md` 与本设计第 8 节；M1 不得编辑 M0 私有文件，也不得将旧 `/api` 的 `reviewer` 字段带入 v1 接口。
