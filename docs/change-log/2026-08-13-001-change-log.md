# 建立仓库内本地修改日志机制

- 变更标识：`2026-08-13-001-change-log`
- 日期：`2026-08-13`
- 状态：`已完成`
- 所属模块：`M7`；协作模块：`无`
- 需求追踪：`NFR-MNT-05`、`NFR-MNT-06`、SRS 14.3 第 6～9 条、SRS 14.5
- 关联记录：`无`

## 改动内容

- 建立仓库内、可被后续开发直接读取的修改日志机制，确保每个逻辑变更单元都有主责模块、需求追踪、文件和数据影响、冲突检查、验证、回滚与待续事项记录。
- 将原有“变更说明”要求扩展为“本地独立日志 + 索引登记”，避免仅在对话、提交说明或临时状态报告中保留修改信息。

## 文件与数据影响

- `docs/change-log/README.md`；新增：说明日志目录的读取、创建、索引和历史更正规则。
- `docs/change-log/INDEX.md`；新增：修改日志的统一入口，并登记本变更。
- `docs/change-log/TEMPLATE.md`；新增：后续所有逻辑变更应复用的记录模板。
- `docs/change-log/2026-08-13-001-change-log.md`；新增：本次机制变更的可追溯记录。
- `docs/requirements/software-requirements-spec.md`；修改：第 14.5 节增加本地日志强制规则和模板字段。
- `README.md`；修改：目录结构和开发约定指向本地修改日志。
- 数据库表、迁移、API、DTO、事件、运行配置：无。

## 依赖与冲突检查

- 已检查：SRS 第 14.1～14.5 节、既有强制变更说明模板、README 开发约定、`docs/` 目录结构；原仓库不存在 `docs/change-log/`。
- 结论：复用既有变更说明字段并补足本地持久化、索引和后续读取规则；不改变 M0～M7 的代码或公开契约，不与现有模块边界冲突。

## 验证与回滚

- 验证：检查索引链接、模板字段与 SRS 第 14.5 节一致；执行 `git diff --check`。
- 回滚：删除 `docs/change-log/` 下本次新增文件，并回退 README 与 SRS 中本次日志机制相关文本；无数据迁移或运行时影响。

## 后续开发提示

- 下一次任何修改开始前，必须先读取 `docs/change-log/INDEX.md` 及与目标模块相关的最近记录；完成后以本模板创建新日志并更新索引。
- 既有 M0 基础设施的历史修改尚未按本机制补录；如需依赖其契约或迁移，应同时查阅 SRS 第 14 节、`backend/README.md` 和现有代码/迁移，不应凭空重建。
