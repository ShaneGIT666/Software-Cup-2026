# 对齐根 README 与当前产品基线

- 变更标识：`2026-08-14-014-readme-baseline-alignment`
- 日期：`2026-08-14`
- 记录状态：`变更已结束`
- 功能验证状态：`未验证`（本次只更新文档基线，不改变任何功能实现或既有验证状态）
- 所属模块：`M7`；协作模块：`M0`～`M6`
- 需求追踪：`AUTH-01`、`AUTH-02`、`API-01`～`API-07`、`NFR-PORT-01`～`NFR-PORT-06`、`NFR-PERF-01`～`NFR-PERF-05`、`NFR-MNT-05`、`NFR-MNT-06`、SRS 第 14.5 节
- 关联记录：`2026-08-14-013-m0-m1-public-integration-gates`

## 改动内容

- 重写根 `README.md`，明确区分原型代码存在、代码已搭建、单元验证、集成验证和功能完成状态。
- 固化网页端 B/S 方向、Windows 默认基线和 Ubuntu Server 24.04 LTS x64 CI 目标，不提供未经验证的 Linux 快速启动命令。
- 对齐 M0～M7 当前进度、旧 `/api` 迁移边界、PostgreSQL 16、受控文件、Worker、Windows Service 和部署验收状态。
- 修正匿名登录、bootstrap CLI、后台 Worker 与普通认证业务接口的边界，避免把非 HTTP 运维流程误写成匿名业务接口。
- 记录当前配置、脚本、Docker、MinerU、向量存储、Playwright、文件格式和验证证据中的已知差异，不提升功能完成状态。
- 补充本地修改日志强制流程、性能阈值、目标上传限制、生产密钥和 Windows 交付工件要求。

## 文件与数据影响

- `README.md`：修改；重写项目入口说明、当前状态、运行方式、配置、安全、开发约定、验证和部署边界。
- `docs/change-log/2026-08-14-014-readme-baseline-alignment.md`：新增；记录本次逻辑变更单元。
- `docs/change-log/INDEX.md`：修改；登记第 014 号记录。
- 数据库、迁移、API、DTO、事件、运行配置和依赖：无。
- 真实数据：无影响。

## 依赖与冲突检查

- 已检查：`docs/change-log/INDEX.md`、第 013 号记录、SRS、M0 公共契约、M1 设计、模块进度、M0/M1 部署方案、根/后端/前端说明、实际路由、配置、存储、上传、CORS、Docker、初始化/验证脚本和前端依赖。
- 结论：README 以 SRS 为目标需求最高基线，并把设计或代码未满足之处标为当前缺口；历史比赛、Linux 和 Docker 材料不作为当前交付承诺。
- 未关闭：SRS 第 14 节“所有生产写操作写 outbox”与部分设计文档“关键领域写操作写 outbox”的口径仍需需求层最终统一；README 暂按 SRS 严格口径记录，并明确当前实现未闭环。

## 验证与回滚

- 验证：人工核对 README 内部链接、目标/当前状态限定、认证例外、事务口径、配置默认值、上传格式和大小、验证脚本实际范围、部署工件及修改日志规则。
- 验证：本次文档写入前复核工作区为空；此前同一审查过程重新执行后端测试为 `250 passed, 25 skipped`，前端构建成功且保留约 1.05 MB 大 chunk 警告。上述结果只支持 README 中带日期的当前记录，不因本次文档变更升级功能状态。
- 回滚：恢复本次变更前的 `README.md`，删除第 014 号日志并移除索引对应行；不涉及数据库或运行数据回滚。

## 后续开发提示

- 下一次修改前先读取 `docs/change-log/INDEX.md`、本记录及受影响模块最新记录。
- 若调整 outbox 适用范围，必须先统一 SRS、M0 公共契约和 M1/M2/M3 设计，再更新 README；不得只修改入口文档。
- README 中列出的当前缺口仍需由对应模块通过真实 PostgreSQL、浏览器、代理、Service、Worker 或 Provider 验收关闭，不能因文档已经对齐而标记完成。
