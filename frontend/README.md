# Frontend

Vue 3 + TypeScript + Vite + Element Plus MVP workspace.

## Local Run

```powershell
cd frontend
npm install
npm run dev
```

The dev server proxies `/api` and `/uploads` to `http://127.0.0.1:8000`.
